# --- Core (default local path runs with just these) ---
google-genai>=1.0.0
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.7.0
pydantic-settings>=2.3.0
rank-bm25>=0.2.2
numpy>=1.26.0
PyYAML>=6.0
requests>=2.31.0          # SEC EDGAR downloads
beautifulsoup4>=4.12.0    # HTML 10-K -> clean text

# --- Testing ---
pytest>=8.0

# --- Optional: PDF document loader ---
# Only needed to ingest .pdf filings. Lazy-imported.
# pypdf>=4.0.0

# --- Optional: cross-encoder reranking (RERANKER_ENABLED=true) ---
# Heavy (pulls torch). Install only if you want second-stage reranking.
# sentence-transformers>=3.0.0

# --- Optional: Supabase pgvector backend (VECTOR_STORE=supabase) ---
# supabase>=2.5.0
